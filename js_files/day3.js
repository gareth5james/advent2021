const fs = require("fs");

const binaryList = fs
  .readFileSync(`${__dirname}/../text_files/day3.txt`, "utf-8")
  .split("\n");

console.log(binaryList);

function task1(binaryList) {
  const gamma = "";
  const epsilon = "";

for(let i = 0)
}